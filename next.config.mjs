/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'xmocfxdxpnmhnibveiib.supabase.co'
            }
        ]
    }
};

export default nextConfig;
